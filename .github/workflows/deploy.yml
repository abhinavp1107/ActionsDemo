name: Deploy Service

on:
  workflow_dispatch:
  workflow_call:

jobs:
  build_package_deploy:
    name: build_package_deploy
    timeout-minutes: 30
    runs-on: ubuntu-latest
    steps:
      - name: Deploy Service
        shell: bash
        run: |
          echo "Service Deployed"
